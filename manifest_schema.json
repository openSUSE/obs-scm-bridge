{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Project Manifest",
  "description": "Schema for _manifest file in pbuild/OBS project git repositories. An empty file is allowed and simply identifies the git repository as package set.",
  "type": ["object", "null"],
  "properties": {
    "packages": {
      "type": ["array", "null"],
      "items": {
        "type": "string",
        "pattern": "^[a-zA-Z0-9\\-_\\+][a-zA-Z0-9.\\-_\\+]*$"
      },
      "description": "List of directories to be used as build package"
    },
    "skip_missing_subdirectories": {
      "type": "boolean"
    },
    "subdirectories": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[a-zA-Z0-9\\-_\\+][a-zA-Z0-9.\\-_\\+/]*$"
      },
      "description": "List of subdirectory paths where each subdirectory inside is considered to be a build package"
    }
  },
  "additionalProperties": false
}
